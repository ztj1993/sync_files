# sync_repo_archive.py 测试方案

## 项目概述
为 `sync_repo_archive.py` 提供完整的单元测试和集成测试，确保仓库 Archive 同步工具的各项功能正常工作。

---

## 测试目标

1. **功能覆盖** - 覆盖所有公共方法和核心逻辑
2. **边界情况** - 处理异常输入和边界条件
3. **模拟依赖** - 使用 Mock 隔离网络请求，确保测试可重复执行
4. **临时文件管理** - 确保测试产生的临时文件及时清理

---

## 测试文件结构

```
c:\Users\ztj\Desktop\file\
├── tests\
│   └── test_sync_repo_archive.py   # 仓库 Archive 同步测试（本文档对应）
├── app\
│   └── sync_repo_archive.py         # 仓库 Archive 同步主脚本
└── .plan\
    ├── 003.sync_repo_archive.md     # 仓库同步需求文档
    └── 004.test_sync_repo_archive.md # 本文档
```

---

## 测试类设计

### TestRepoInfo - 仓库信息类测试

| 测试方法 | 测试内容 |
|----------|----------|
| `test_get_archive_url_github` | GitHub archive URL 生成 |
| `test_get_archive_url_github_head` | GitHub HEAD archive URL |
| `test_get_archive_url_gitee` | Gitee archive URL 生成 |
| `test_get_archive_url_gitlab` | GitLab archive URL 生成 |
| `test_get_archive_url_gitea` | Gitea archive URL 生成 |
| `test_get_commit_api_url_github` | GitHub commit API URL 生成 |
| `test_get_commit_api_url_gitee` | Gitee commit API URL 生成 |
| `test_get_commit_api_url_gitlab` | GitLab commit API URL 生成 |
| `test_get_commit_api_url_gitea` | Gitea commit API URL 生成 |
| `test_get_unique_key` | 唯一 key 生成 |

### TestRepoArchiveSync - 核心功能测试

| 测试方法 | 测试内容 |
|----------|----------|
| `test_sanitize_filename` | 文件名安全处理（特殊字符替换、长度限制） |
| `test_get_state_file_path` | 状态文件路径生成 |
| `test_save_and_load_state` | 状态保存和加载 |
| `test_load_nonexistent_state` | 加载不存在的状态文件 |
| `test_load_corrupted_state` | 加载损坏的 JSON 状态文件 |
| `test_parse_repo_url_github` | 解析 GitHub URL |
| `test_parse_repo_url_github_with_git_suffix` | 解析带 .git 后缀的 URL |
| `test_parse_repo_url_gitee` | 解析 Gitee URL |
| `test_parse_repo_url_gitlab` | 解析 GitLab URL |
| `test_parse_repo_url_gitea` | 解析 Gitea URL |
| `test_parse_repo_url_invalid` | 解析无效 URL |
| `test_http_get` | HTTP GET 请求 |
| `test_http_get_json` | HTTP GET JSON 请求 |
| `test_get_latest_commit_github` | 获取 GitHub 最新 commit |
| `test_get_latest_commit_gitee` | 获取 Gitee 最新 commit |
| `test_get_latest_commit_gitlab` | 获取 GitLab 最新 commit |
| `test_get_latest_commit_gitea` | 获取 Gitea 最新 commit |
| `test_download_archive` | 下载 archive |
| `test_extract_archive_zip` | 解压 zip 文件 |
| `test_get_archive_format` | 获取 archive 格式 |
| `test_sync_repo_no_update` | 仓库无更新场景 |
| `test_sync_repo_with_update` | 仓库有更新场景 |
| `test_sync_repo_no_extract` | 仓库不解压场景 |
| `test_sync_repo_custom_extract_dir` | 自定义解压目录 |
| `test_sync_repo_api_error` | API 错误处理 |
| `test_sync_multiple_repos` | 批量同步多个仓库 |
| `test_sync_multiple_repos_with_extract_config` | 批量同步带解压配置 |

### TestIntegration - 集成测试

| 测试方法 | 测试内容 |
|----------|----------|
| `test_full_workflow` | 完整工作流程：首次同步 → 无更新 → 有更新 |

### TestMainFunction - 主函数测试

| 测试方法 | 测试内容 |
|----------|----------|
| `test_main_help` | 帮助信息输出 |
| `test_main_no_args` | 无参数处理 |
| `test_main_single_repo` | 单仓库模式 |
| `test_main_config_file_not_exist` | 配置文件不存在 |
| `test_main_invalid_config_format` | 无效 JSON 格式 |
| `test_main_config_not_list` | 配置非列表格式 |
| `test_main_valid_config` | 有效配置文件 |

---

## 测试技术要点

### 1. Mock 网络请求
使用 `unittest.mock.patch` 模拟 `urlopen` 和内部方法，避免真实网络请求：

```python
@patch.object(sync_repo_archive, 'urlopen')
def test_download_archive(self, mock_urlopen):
    mock_response = MagicMock()
    mock_response.read.side_effect = [b'content', b'']
    mock_urlopen.return_value.__enter__.return_value = mock_response
    # ... 测试代码
```

### 2. 临时文件清理
- **类级别**：`setUp()` 创建临时目录，`tearDown()` 使用 `shutil.rmtree()` 清理
- **方法级别**：使用 `self.addCleanup(os.unlink, config_path)` 确保临时配置文件被删除

### 3. 日志控制
测试开始时禁用日志输出，避免干扰：
```python
logging.disable(logging.CRITICAL)
```

### 4. 模块导入
由于测试文件位于 `tests/` 目录，需要动态导入父目录的模块：
```python
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import importlib.util
spec = importlib.util.spec_from_file_location("sync_repo_archive", ...)
```

---

## 运行方式

```bash
# 从项目根目录运行
python tests/test_sync_repo_archive.py

# 运行特定测试类
python tests/test_sync_repo_archive.py TestRepoInfo

# 运行特定测试方法
python tests/test_sync_repo_archive.py TestRepoArchiveSync.test_sync_repo_no_update

# 显示详细输出
python tests/test_sync_repo_archive.py -v
```

---

## 测试统计

| 类别 | 数量 |
|------|------|
| RepoInfo 类测试 | 10 |
| RepoArchiveSync 核心测试 | 26 |
| 集成测试 | 1 |
| 主函数测试 | 7 |
| **总计** | **44** |

---

## 依赖说明

纯 Python 标准库实现，无需第三方依赖：
- `unittest` - 测试框架
- `unittest.mock` - Mock 对象
- `tempfile` - 临时文件/目录
- `shutil` - 目录清理
- `zipfile` - 创建测试 zip 文件
- `importlib.util` - 动态模块导入

---


